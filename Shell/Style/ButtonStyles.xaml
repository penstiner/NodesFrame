<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══════════════════════════════════════ -->
    <!--  工具栏 / 编辑器按钮样式                -->
    <!-- ══════════════════════════════════════ -->

    <Style x:Key="ToolBtnStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource ToolboxItemForeground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="bd" Background="{TemplateBinding Background}"
                            CornerRadius="3" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#33FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#22FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource ToolboxItemDescForeground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentBtnStyle" TargetType="Button" BasedOn="{StaticResource ToolBtnStyle}">
        <Setter Property="Background" Value="#FF0078D4"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF1A8CD8"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- 执行按钮 → 绿色实底 -->
    <Style x:Key="SuccessAccentBtnStyle" TargetType="Button" BasedOn="{StaticResource ToolBtnStyle}">
        <Setter Property="Background" Value="#FF43A047"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#FF66BB6A"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DangerBtnStyle" TargetType="Button" BasedOn="{StaticResource ToolBtnStyle}">
        <Setter Property="Foreground" Value="#FF6B6B"/>
    </Style>

    <!-- ═══ FontAwesome 图标按钮 ═══ -->
    <Style x:Key="IconBtnStyle" TargetType="Button" BasedOn="{StaticResource ToolBtnStyle}">
        <Setter Property="FontFamily" Value="{StaticResource FontAwesome}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="28"/>
    </Style>

    <!-- 保存/加载 → 蓝色 -->
    <Style x:Key="IconBtnAccentStyle" TargetType="Button" BasedOn="{StaticResource IconBtnStyle}">
        <Setter Property="Foreground" Value="#FF42A5F5"/>
    </Style>

    <!-- 清空/删除 → 红色 -->
    <Style x:Key="IconBtnDangerStyle" TargetType="Button" BasedOn="{StaticResource IconBtnStyle}">
        <Setter Property="Foreground" Value="#FFEF5350"/>
    </Style>

    <!-- 执行 → 绿色 -->
    <Style x:Key="IconBtnSuccessStyle" TargetType="Button" BasedOn="{StaticResource IconBtnStyle}">
        <Setter Property="Foreground" Value="#FF66BB6A"/>
    </Style>

    <!-- 主题/工具 → 橙色 -->
    <Style x:Key="IconBtnWarnStyle" TargetType="Button" BasedOn="{StaticResource IconBtnStyle}">
        <Setter Property="Foreground" Value="#FFFFA726"/>
    </Style>

    <!-- 自动布局 → 紫色 -->
    <Style x:Key="IconBtnPurpleStyle" TargetType="Button" BasedOn="{StaticResource IconBtnStyle}">
        <Setter Property="Foreground" Value="#FFAB47BC"/>
    </Style>

    <Style x:Key="AboutButtonStyle" TargetType="Button">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="#5D6B99"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="MinWidth" Value="75"/>
        <Setter Property="Height" Value="65"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="root" Background="{TemplateBinding Background}" Padding="3">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Name="icon" Text="{TemplateBinding Tag}"
                                Foreground="#5D6B99" FontSize="32"
                                VerticalAlignment="Center" HorizontalAlignment="Center"
                                FontFamily="{StaticResource FontAwesome}"/>
                            <TextBlock Grid.Row="1" Name="header" Text="{TemplateBinding Content}"
                                Foreground="#5D6B99"
                                FontSize="{TemplateBinding FontSize}" Margin="3"
                                VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2216a1ff" TargetName="root"/>
                            <Setter Property="BorderBrush" Value="#4416a1ff" TargetName="root"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#2216a1ff" TargetName="root"/>
                            <Setter Property="BorderBrush" Value="#4416a1ff" TargetName="root"/>
                            <Setter Property="Foreground" TargetName="icon" Value="orange"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ══════════════════════════════════════ -->
    <!--  对话框按钮样式                          -->
    <!-- ══════════════════════════════════════ -->

    <Style x:Key="DialogConfirmBtn" TargetType="Button">
        <Setter Property="Background" Value="#FF0078D4"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Bd" Background="{TemplateBinding Background}"
                            CornerRadius="6" Padding="22,8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FF1A8CD8"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FF0060B0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DialogCancelBtn" TargetType="Button">
        <Setter Property="Background" Value="#FF3A3A4A"/>
        <Setter Property="Foreground" Value="#C0C0D0"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Bd" Background="{TemplateBinding Background}"
                            CornerRadius="6" BorderBrush="#FF555570"
                            BorderThickness="1" Padding="22,8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FF4A4A5A"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DialogCloseBtn" TargetType="Button">
        <Setter Property="Foreground" Value="#888"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="Bd" Background="Transparent" CornerRadius="0,8,0,0"
                            Width="40" Height="40">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FFE81123"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
